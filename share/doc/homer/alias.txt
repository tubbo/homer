homer alias NAME [COMMAND] [options]

Saves an alias to ~/etc/aliases.zsh for future shell sessions. Has the
option of removing the file and customizing the commit message.

OPTIONS:

  -r    Removes the alias (by name) from ~/etc/aliases.zsh

  -m MESSAGE    an optional message to pass to the Git commit
