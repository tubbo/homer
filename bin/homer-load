#!/bin/zsh

# Load common profile configuration
for file in `ls $HOMER_PATH/etc/profile.d`; do
  source "$HOMER_PATH/etc/profile.d/$file"
done

# Load user's local profile configuration
for file in `ls $HOME/etc/profile.d`; do
  source "$HOME/etc/profile.d/$file"
done
