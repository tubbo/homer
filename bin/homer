#!/bin/zsh
#
# Executes Homer commands

if [[ -z $HOMER_PREFIX ]]; then
  HOMER_PREFIX=$(brew --prefix homer)
fi
HOMER_COMMAND=$1
HOMER_EXECUTABLE=$HOMER_PREFIX/bin/homer-$HOMER_COMMAND

if [[ -e $HOMER_EXECUTABLE ]]; then
  $HOMER_EXECUTABLE
else
  $HOMER_PREFIX/bin/homer-help
fi
